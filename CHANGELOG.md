# Change Log

## 0.0.1

- Initial Quick JSON Viewer scaffold.
- Opens `.json` files larger than the configured threshold in a readonly,
  searchable raw-text preview showing the first configured number of lines.
- Adds a `Show raw JSON` action that reopens the file with VS Code's default
  editor.
